{
  "name": "micro-js-all-in-one-example",
  "dependencies": {
    "micro-js": "file:../.."
  },
  "scripts": {
    "start": "export MICRO_REGISTRY_URL=localhost:10000 && node index.js",
    "build": "docker build -f Dockerfile ../.."
  }
}
